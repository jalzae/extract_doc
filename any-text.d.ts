declare module 'any-text' {
  function getText(filePath: string): Promise<string>; // Adjust the type based on the actual behavior of getText
  // Add any other types or functions used from 'any-text'
}